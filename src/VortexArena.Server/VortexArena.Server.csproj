<Project Sdk="Microsoft.NET.Sdk">
  <!-- Server-only orchestration (the C# analogue of qcsrc/server/): the game world loop, client
       management, scores, teamplay, round/match flow, voting, and the bot AI. Godot-free so the
       dedicated server runs headless (ADR-0008/0012). References Common (gameplay) + Engine (sim). -->
  <PropertyGroup>
    <TargetFramework>$(VortexArenaTfm)</TargetFramework>
    <RootNamespace>VortexArena.Server</RootNamespace>
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="..\VortexArena.Common\VortexArena.Common.csproj" />
    <ProjectReference Include="..\VortexArena.Engine\VortexArena.Engine.csproj" />
  </ItemGroup>
</Project>
